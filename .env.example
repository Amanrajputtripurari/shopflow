# ShopFlow — Environment (dev only)

# Optional dev override. Production uses Setup Wizard → encrypted store in main process.
# Never commit real credentials.

MONGODB_URL=mongodb://localhost:27017/shopflow

# NODE_ENV is set by scripts; do not rely on manual changes in production builds.
